// Draggable
const draggableSnapshot = {
  isDragging: true,
  draggingOver: "column-1", // "column-1" is the "id" of the droppable
};

// Droppable
const droppableSnapshot = {
  isDraggingOver: true,
  draggingOverWith: "tast-1", // "task-1" is the "id" of the draggable, this will be set to null if the droppable is not being dragged over
};
